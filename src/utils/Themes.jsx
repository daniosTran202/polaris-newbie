const theme = {
  colors: {
    // replace these examples override color values
    surface: "rgb(0,0,0)",
    onSurface: "rgb(33,43,54)",
  },
  config: {
    // add config overrides here
  },
};

module.exports = theme;
